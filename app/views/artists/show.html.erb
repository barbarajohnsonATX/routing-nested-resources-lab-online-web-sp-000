<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <!-- <li><%= song.title %></li> -->
    <li> <%= link_to song.title, artist_song(@artist, song) %> </li>
    
  <% end %>
</ul>
